import Foundation

// Copie ce fichier en "Secrets.swift" (gitignored) et renseigne tes propres
// valeurs, disponibles dans Supabase > Project Settings > API.
enum SupabaseConfig {
    static let url = URL(string: "https://YOUR-PROJECT.supabase.co")!
    static let anonKey = "YOUR-SUPABASE-ANON-KEY"
}
